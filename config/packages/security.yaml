# config/packages/security.yaml
security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        login:
            pattern: ^/api/login
            stateless: true
            json_login:
                check_path: /api/login_check
                username_path: email
                password_path: password
                success_handler: lexik_jwt_authentication.handler.authentication_success
                failure_handler: lexik_jwt_authentication.handler.authentication_failure

        api:
            pattern: ^/api
            stateless: true
            provider: app_user_provider
            jwt: ~

        main:
            lazy: true
            provider: app_user_provider

    access_control:
        - { path: ^/api/login_check,    roles: PUBLIC_ACCESS }
        - { path: ^/api/docs,           roles: PUBLIC_ACCESS }
        # — Ressources publiques —
        - { path: ^/api/products,       roles: PUBLIC_ACCESS }
        - { path: ^/api/images,         roles: PUBLIC_ACCESS }
        - { path: ^/api/platforms,      roles: PUBLIC_ACCESS }
        # — Inscription publique —
        - { path: ^/api/users,           roles: PUBLIC_ACCESS, methods: [ POST ] }
        # — Ressources privées —
        - { path: ^/api/invoices,       roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/api/payment_methods,roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/api/product_keys,   roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/api/users,          roles: IS_AUTHENTICATED_FULLY }
        # — Tout le reste —
        - { path: ^/api,                roles: IS_AUTHENTICATED_FULLY }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
